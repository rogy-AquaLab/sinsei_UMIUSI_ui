{
  "name": "sinsei-umiusi-terminal-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.13"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "test": "bun test",
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "hash-password": "bun src/hashPassword.ts"
  },
  "dependencies": {
    "@sinsei-umiusi/terminal-protocol": "workspace:*",
    "hono": "^4.12.32",
    "rate-limiter-flexible": "^11.2.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.13",
    "typescript": "~5.9.3"
  }
}
